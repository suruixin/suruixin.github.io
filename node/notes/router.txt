router  - 路由
把不同的目录对应到不同的模块

------------------------------------------------------------------------------------------

router.get();
router.posh();
router.use();

------------------------------------------------------------------------------------------

1、创建router
	var router = express.Router();
2、把router添加到server;
	server.use('/user',router);
3、router内部
	router.get('文件',function(req,res){});
	router.posh('文件',function(req,res){});
	router.use('文件',function(req,res){});

------------------------------------------------------------------------------------------