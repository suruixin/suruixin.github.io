模板引擎
jade	破坏式  侵入式、强依赖
ejs	非侵入式，弱依赖


jade
------------------------------------------------------------------------

用法：
html
	head
		style
	body
		div

1、根据缩进，规定层级
2、属性放在()里面，逗号分割
3、内容空个格 然后直接写

属性
script(src="",ref="")

内容
div 内容

------------------------------------------------------------------------

consolidate  适配

------------------------------------------------------------------------

总结
模板引擎 ：适配
1、consolidate
	server.set('view engine','html');   //要输出什么
	server.set('views','模板文件目录');	// 从那里找模板  模板叫什么
	server.engine('html',consolidate.ejs);	//输出整合后的文件
	server.get('/',function(req,res){
		req.render('模板文件',数据)
	})