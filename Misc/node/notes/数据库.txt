数据库

------------------------------------------------------------------------------------------

MySQL,Oracle,SQLServer,Access,db2,fox pro

MySQL:
	免费、中小网站首选
	优点：性能还好
	缺点：集群、容灾微弱

Oracle:
	优点：性能非常好、集群，容灾非常强
	缺点：贵

文件型数据库：
	sqlite,mongodb

------------------------------------------------------------------------------------------

Server端：   //服务端
	MySQL

client端：	 //客户端
	Navicat  管理工具
	NodeJS	 程序

------------------------------------------------------------------------------------------

数据基本概念
两种单位
	1、库：文件夹-用来管理，本身没法存数据
	2、表：文件-存数据

表：
	行  一条数据
	列（字段，域）	一个数据项

------------------------------------------------------------------------------------------

SQL:
4大查询语句 增删改查

	增 INSERT
		INSERT INTO 表 (字段列表) VALUES(值列表)
		INSERT INTO `user_table` (`ID`,`username`,`password`) VALUES(0,'srx','123456');
	删 DELETE
		DELETE FROM 表 WHERE 条件
	改 UPDATE
		UPDATE 表 SET 字段=值,字段=值,字段=值,..... WHERE 条件 
	查 SELECT
		SELECT 什么 FROM 表
		SELECT * FROM `user_table`
SQL标准写法
1、关键字大写
3、库、表、字段需要加上``

------------------------------------------------------------------------------------------

子句:
WHERE 条件

WHERE name="srx"
WHERE age>= 18
WHERE age>18 AND name="srx"
WHERE age>18 OR name="srx"

------------------------------------------------------------------------------------------

ORDER 排序
ORDER BY age ASD/DESC,name ASD/DESC
	ASC - 升序
	DESC - 降序

------------------------------------------------------------------------------------------

GROUP 聚类-合并数据 
SELECT 字段 FROM 表单名 GROUP BY 字段

SELECR 字段,COUNT(字段) FROM 表单名 GROUP BY 字段   //求和

	COUNT/MIN/MAX/AVG

------------------------------------------------------------------------------------------

LIMIT 限制输出(分页)

	
------------------------------------------------------------------------------------------ 

子句之间是有顺序的
WHERE GROUP ORDER LIMIT